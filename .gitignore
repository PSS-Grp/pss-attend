__pycache__/
*.pyc

# ローカル実行・動作確認用のSQLiteデータベースファイル
# （DATABASE_URL未設定時のフォールバックとして、Colab/ローカル実行時に
# ATTENDANCE_DATA_DIR配下に作られるもの。本番(Render)ではNeon(PostgreSQL)を
# 使うため、このファイルは通常のRender運用では作られない）。
attendance_fixed/db/*.db

.env
