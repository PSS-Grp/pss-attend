__pycache__/
*.pyc

# ローカル実行・動作確認用のSQLiteデータベースファイル。
# 本番(Render)ではATTENDANCE_DATA_DIR配下（永続ディスク）に作られるため、
# リポジトリ内のこのパスにはコミットしない。
attendance_fixed/db/*.db

# 手配書のアップロード画像（ローカル実行時にできるもの）。
# 本番ではATTENDANCE_DATA_DIR配下（永続ディスク）に保存される。
attendance_fixed/uploads/

.env
